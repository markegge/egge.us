{
  // Static-assets-only Worker: no server code, just serves the built site.
  // `npm run build` emits ./dist, and `npx wrangler deploy` uploads it.
  "name": "egge-us-site",
  "compatibility_date": "2026-06-05",
  "assets": {
    "directory": "./dist"
  }
}
